
/**
 * HashMap Factory
 * Class in charge of implementing the HashMap|HashTable Data Structure.
 */
const hashmapFactory = () => {
  // the total number of Buckets the HashMap will start with
  const __CAPACITY = 16;

  // 
  let __buckets = Array.from()


  /* **************
   * MODULE BUILD *
   ************** */
  return Object.freeze({

  });
};




/**
 * Module Exports
 */
export default hashmapFactory;
